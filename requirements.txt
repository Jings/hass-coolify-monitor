# Runtime dependencies for the integration, kept in sync with
# custom_components/coolify_monitor/manifest.json's "requirements" list.
#
# Currently empty: the integration only uses aiohttp, already provided by Home Assistant core.
